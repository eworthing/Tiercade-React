/**
 * Bundled/default tier list project
 */

import defaultProjectData from "./defaultProject.json";
import { Project } from "../project";

export const DEFAULT_PROJECT: Project = defaultProjectData as Project;
